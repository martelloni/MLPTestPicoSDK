/* Keep executable code in the normal flash/XIP + RAM load path. Banked SRAM is reserved
 * for private data/stack only and must not contain initialized code sections, because the
 * UF2 format cannot represent executable contents in SRAM as a flash image.
 */
SECTIONS
{
    /* Intentionally empty: code remains in the standard .text / flash-backed sections. */
}
